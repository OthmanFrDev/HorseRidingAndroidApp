<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.horseriding">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HorseRiding"
        android:usesCleartextTraffic="true">

        <activity android:name=".EditNote"></activity>
        <activity android:name=".NoteController" >

        </activity>
        <activity android:name=".EditFormUser" />
        <activity
            android:name=".dialog_user_details"
            tools:ignore="Instantiatable" />
        <activity android:name=".Month_view" />

        <activity android:name=".DateTimePicker" />
        <activity android:name=".WeekView_Calendar" />
        <activity android:name=".RecycleCalendar" />
        <activity android:name=".ListActivity" />
        <activity android:name=".SeanceController" />
        <activity android:name=".ligne_List_User_Activity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".UserController" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>